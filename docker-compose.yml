services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: kfokam48_fullstack
      POSTGRES_PASSWORD: "1234"
    ports:
      - "5432:5432"
    volumes:
      - kfokam48-db:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  kfokam48-db:
